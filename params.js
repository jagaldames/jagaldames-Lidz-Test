// Editing this values affects the score calculation of the clients

const price = 3000;
const upfront = 600;
const interest = 0.05;
const ufvalue = 30000;

module.exports = {
    price,
    upfront,
    interest,
    ufvalue
};